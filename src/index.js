import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
  <div>Hi, It's App with Dev SERVER 💩!</div>
);

ReactDOM.render(
  <App />, 
  document.getElementById('root')
);
